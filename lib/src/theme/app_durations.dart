class AppDurations {
  const AppDurations._();

  static const autoAttackInstant = Duration.zero;
  static const autoAttackFast = Duration(milliseconds: 250);
  static const autoAttackNormal = Duration(milliseconds: 750);
  static const autoAttackSlow = Duration(milliseconds: 1500);
}
